name: keyah
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  sqflite: ^2.2.8+4
  path: ^1.8.3
  path_provider: ^2.1.1
  url_launcher: ^6.2.1
  cupertino_icons: ^1.0.8
  curved_navigation_bar: ^1.0.3
  upgrader: ^12.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
      - assets/database/keyah_database.db
      - assets/qrs/
      - assets/images/